Square checkbox with a right-side label; teal fill + white tick when on.

```jsx
<Checkbox label="Only show students who need attention" defaultChecked />
<Checkbox label="Include consent-gated minors" disabled />
```

Controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`).
