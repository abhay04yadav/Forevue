Quiet on/off toggle for settings and filters; teal track when on.

```jsx
<Switch label="Email me weekly risk digests" defaultChecked />
```

Controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`).
