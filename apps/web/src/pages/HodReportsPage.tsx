import { WorkspacePlaceholderPage } from "@/pages/WorkspacePlaceholderPage";

export function HodReportsPage() {
  return (
    <WorkspacePlaceholderPage
      eyebrow="Department"
      title="Reports & notices"
      subtitle="Department report drafts and notice generators will appear here once the reporting module ships."
    />
  );
}
