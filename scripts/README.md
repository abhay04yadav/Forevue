# Scripts (`scripts/`)

**Status:** Skeleton.

Repeatable dev/ops automation: database seed data, OpenAPI→TypeScript codegen,
local bootstrap helpers. Scripts that touch tenant data must respect RLS and
audit conventions.
